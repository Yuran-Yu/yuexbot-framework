# YuexBot v1.0.1 使用说明

YuexBot 是一个多账号 OneBot 11 QQ 机器人框架客户端，当前主线兼容 NapCat 和 LLBot 的正向 WebSocket、反向 WebSocket，以及部分 HTTP API 兜底。

## 目录结构

正式包采用根目录 `YuexBot.exe` + `main` 文件夹结构：

```text
YuexBot.exe
main/
  bin/      JadeView_x64.dll 等运行 DLL
  plugin/   插件 DLL / JSON
  UI/       框架内置 UI 释放出的 index.html
  corn/     账号数据，例如 accounts.json
  data/     框架设置、运行数据、日志、插件配置
  sdk/      原生插件 SDK
```

启动入口：

```text
YuexBot.exe
```

## 首次使用

首次使用建议进入「账号」页面添加账号：

- 正向 WebSocket：框架主动连接 NapCat/LLBot。
- 反向 WebSocket：NapCat/LLBot 主动连接框架监听端口。
- HTTP POST：用于事件 HTTP 上报场景。

每个账号的连接方式是独立配置的。账号开启「启动自动连接」后，下次打开框架会自动连接该账号。

## 主要功能

- 仪表盘：查看账号数量、插件数量、消息量、运行状态和连接信息。
- 账号：添加、编辑、删除、连接、断开账号。
- 插件：添加 DLL、启用、禁用、重载、删除、查看权限、打开插件设置；启用后默认接收全部消息和事件。
- 群组：按当前账号加载群列表，支持查看成员和发送群消息。
- 好友：按当前账号加载好友列表，支持详情、发送消息、点赞、删除。
- 消息：按账号、事件类型、群号、群名、发送者 QQ/昵称筛选消息事件。
- 日志：查看系统、消息、通知、请求、错误等日志，支持搜索、详情、复制、导出筛选结果。
- 设置：配置全局反向监听端口、正向连接模板、HTTP POST、自动连接账号开关等。

## 插件说明

当前正式版推荐 YuexBot 原生 x64 DLL 插件，SDK 文档在 `main/sdk` 目录：

- `sdk/yuex_plugin_sdk.h`
- `sdk/README.md`
- `sdk/开发指南.md`
- `sdk/plugin_template.cpp`
- `sdk/plugin_template.json`

插件 DLL 放入 `main/plugin` 目录；插件配置和数据由框架保存到 `main/data/plugins`。

小栗子插件兼容层目前暂停作为备用方案，不作为当前正式版主线加载方式。

## 注意事项

- 当前主程序是 x64，插件也必须是 x64 DLL。
- 如果正向 WS 只能收到事件但 API 无响应，请在账号配置中填写 HTTP API 兜底端口。
- 账号卡片会显示事件通道、API 通道、群/好友数量、延迟和最近连接错误，便于定位连接问题。
- 导出的日志使用 UTF-8 BOM，方便 Windows 记事本直接查看中文。
